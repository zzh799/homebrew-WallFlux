# Homebrew Tap: WallFlux

WallFlux 的 Homebrew Cask tap。

## 安装

```bash
brew install zzh799/WallFlux
```

## 更新

```bash
brew upgrade wallflux
```

Cask 由 GitHub Actions 定时同步（每 30 分钟检查一次），新版本发布后会自动更新。
